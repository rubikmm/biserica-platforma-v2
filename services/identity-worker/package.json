{
  "name": "@xc/identity-worker",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "dependencies": {
    "@xc/config": "workspace:*",
    "@xc/contracts": "workspace:*",
    "@xc/db": "workspace:*",
    "@xc/observability": "workspace:*",
    "zod": "^4.1.5"
  }
}
